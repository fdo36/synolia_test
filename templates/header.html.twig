{% extends 'base.html.twig' %}

{% block header %}
    <a href="{{ path('index') }}">Home</a> | <a href="{{ path('twig') }}">Twig</a> | <a href="{{ path('user-index') }}">IP entities</a> 
    | <a href="{{ path('contact') }}">Contact</a>
{% endblock %}